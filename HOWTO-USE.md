# DeepTalk Self 详细使用指南

本指南涵盖三种使用方式：作为 Hermes Agent Skill、作为通用 AI 提示词、离线自问自答。选择适合你的方式。

---

## 方式一：作为 Hermes Agent Skill（推荐）

这是最完整的体验——AI 会自动管理进度、切换追问模式、校验数据、生成画像。

### 安装

```bash
# 克隆仓库到你的 Hermes skills 目录
git clone https://github.com/andyjun1229/DeepTalk-Self.git ~/.hermes/skills/deeptalk-self
```

Hermes Agent 会自动识别该 skill。

### 初始化

开始前需要初始化进度文件：

```bash
cd ~/.hermes/skills/deeptalk-self
python3 -c "
import json
questions = []
dims = ['决策逻辑','人际交往','学习成长','价值观动机','自我认知','家庭与身份认同','能力适配']
topics = {
    '决策逻辑': ['重要决策案例','信息来源','失败复盘'],
    '人际交往': ['关系建立','冲突处理','影响力'],
    '学习成长': ['学习方式','能力迁移','成长速度'],
    '价值观动机': ['价值排序','驱动力','矛盾点'],
    '自我认知': ['自我评价','防御机制','盲点'],
    '家庭与身份认同': ['原生家庭结构','家庭叙事的影响','身份认同与自我定义'],
    '能力适配': ['优势识别','工作偏好','成就事件']
}
for d in dims:
    for t in topics[d]:
        for n in range(1,4):
            prefix = {'决策逻辑':'D1','人际交往':'D2','学习成长':'D3','价值观动机':'D4','自我认知':'D5','家庭与身份认同':'D7','能力适配':'D6'}[d]
            label = {'重要决策案例':'A','信息来源':'B','失败复盘':'C',
                     '关系建立':'A','冲突处理':'B','影响力':'C',
                     '学习方式':'A','能力迁移':'B','成长速度':'C',
                     '价值排序':'A','驱动力':'B','矛盾点':'C',
                     '自我评价':'A','防御机制':'B','盲点':'C',
                     '原生家庭结构':'A','家庭叙事的影响':'B','身份认同与自我定义':'C',
                     '优势识别':'A','工作偏好':'B','成就事件':'C'}[t]
            questions.append({'id': f'{prefix}-{label}{n}', 'dimension': d, 'topic': t, 'status': 'pending'})
with open('data/interview-progress.json','w') as f:
    json.dump({'version':'3.0','total_questions':63,'questions':questions}, f, ensure_ascii=False, indent=2)
os.makedirs('data/interviews', exist_ok=True)
print('初始化完成：63个问题已注册')
"
```

### 使用

在 Hermes Agent 对话中说出触发词：

- **「开始深度访谈」** — 从第一个 pending 问题启动
- **「继续访谈」** — 从上次中断的位置继续
- **「访谈进度」** — 查看当前完成情况
- **「跳到 D7-A1」** — 跳转到指定问题
- **「生成画像」** — 所有63题完成后生成个人操作手册

AI 会自动：
1. 读取进度文件，找到第一个 pending 问题
2. 从 `references/framework-full.md` 中读取问题原文
3. 问出问题
4. 根据你的回答状态，从 `references/probe-kitchen.md` 选择追问模式
5. 识别防御信号（`references/defense-patterns.md`）
6. 保存回答到 `data/interviews/`
7. 运行 `validate_progress.py` 校验
8. 63题完成后运行 `gen_profile.py` 生成个人操作手册

### 问答示例

```
你：开始深度访谈

AI：当前进度：0/63。
第一个问题：

[D1-A1] 过去5年，你觉得最正确的决定是什么？当时为什么不那么确定？

你：[你的回答]

AI：[保存回答，根据回答状态切换模式]

## ✓ 已回答已保存
data/interviews/2026-07-19--决策逻辑--重要决策案例--D1-A1.md

## 当前进度
D1 决策逻辑：1/9
D2 人际交往：0/9
...

## 下一个问题
[D1-A2] 有没有哪个决定，你现在回想起来"当时要是那样就好了"？
```

---

## 方式二：作为通用 AI 提示词

如果你不使用 Hermes Agent，可以把 SKILL.md 发给任意 AI 助手，说"开始深度访谈"。

### 准备工作

将以下文件内容准备好：
- `SKILL.md` — 发给 AI 作为系统指令
- `references/framework-full.md` — AI 会从中读取问题
- `references/probe-kitchen.md` — AI 会从中选择追问句式
- `references/defense-patterns.md` — AI 会用它识别防御

### 使用

1. 把 SKILL.md 发给 AI 作为第一条消息
2. 说"开始深度访谈"
3. AI 会按框架提问
4. 你逐条回答
5. 重要：**需要你自己记录回答**（没有自动保存功能）
6. 完成后，将你记录的全部回答发给 AI，让它生成操作手册

### 局限性

- 无法自动保存回答到本地文件
- 无法运行校验脚本（依赖于 AI 自己检查）
- 没有进度 JSON，跨会话能力取决于 AI 的上下文记忆
- 画像生成需要你把完整对话记录拷贝出来发给 AI

推荐每次访谈后把你和 AI 的对话导出保存，方便后续生成画像。

---

## 方式三：离线自问自答

不需要 AI，一份问题列表 + 一个笔记本。

### 准备

打印或打开 `references/framework-full.md`，准备好纸笔或笔记软件。

### 使用

1. 每天回答 3-5 个问题（约 20-30 分钟）
2. 每个问题至少写 200 字的真实经历，不要概括，写具体事件
3. 给自己设置硬约束：**不写"我觉得"，写"我当时做了"**
4. 按维度推进：建议顺序 → D1 → D2 → D3 → D6 → D4 → D5 → D7
5. 大约 2-3 周完成全部 63 题

### 自我分析

全部写完后，你不需要 AI 也能自己分析：

1. **读一遍所有回答**，标记出矛盾点（前后不一致的地方）
2. **找出反复出现的模式**（同样的困境在不同维度的回答里出现）
3. **写一段自我总结**：我是谁、我核心的矛盾是什么、我想改变什么

如果想生成结构化的操作手册，可以参考 `scripts/gen_profile.py` 的逻辑，手动对照你的回答填入对应章节。

---

## 问题回答技巧

### 怎么答才有效

| 不要 | 要 |
|------|-----|
| "我觉得我比较随和" | "上次团队争论时，我选择了沉默，因为…" |
| "我可能有点完美主义" | "上周我为了一个错别字重写了整篇文档" |
| "我父母对我很好" | "我小时候发烧，我妈半夜背我去医院，路上摔了一跤" |

**一条好回答 = 一个具体事件 + 当时你是怎么想的 + 事后你怎么看。**

### 回答深度自检

答完一个问题后问自己这三个问题：

1. 我提到了具体的人、时间、地点吗？（如果都是抽象的→不够深）
2. 我说出了当时真实的念头吗？还是事后总结的好听版本？
3. 这个回答让我自己有什么新发现吗？

如果三个答案都是"否"，这个回答的质量不够。重新想一个具体事件再答。

### 跳过规则

如果你真的不想回答某个问题：
- 直接说"跳过这个问题"
- 不用解释原因
- 累计跳过 3 次后，系统会问你是永久跳过还是后面再补

---

## 输出解读

63 题全部完成后，生成一份「个人操作手册」。这不是心理测评报告，是**一份可执行的理解自己的手册**。

### 最重要的章节：核心矛盾

手册会列出 4-6 组你内在的矛盾。每组合：

```
### 矛盾标题
描述这个矛盾的本质

- **冲突场景：** 这种矛盾在什么情况下会爆发
- **止损规则：** 当矛盾爆发时你应该怎么做
```

**请记住：** 矛盾不是"问题"——是你的操作系统里同时运行着两套逻辑。意识到它们的存在，比消除它们重要。

### 常见反应

- **"这不就是我吗"** — 正常。这些素材全部是你自己说出来的
- **"这个止损规则我做不到"** — 也没关系。列出来是为了让你在冲突发生时有选项，不是强制你执行
- **"我觉得我没那么矛盾"** — 试试在真实场景中观察自己一周，你会发现的
- **"我想改"** — 手册不提供解决方案。它提供的是定位——你想改什么，要先知道你在哪

---

## 故障排除

| 问题 | 原因 | 解决 |
|------|------|------|
| AI 一次问了两个问题 | LLM 没遵守约束 | 回答第一个，然后说"先答这个，下一个没答" |
| AI 开始分析我的心理 | LLM 切换到了分析模式 | 说"不要分析，继续问下一题" |
| AI"我理解你" | LLM 滑入共情模式 | 说"不要共情，你是访谈者不是朋友" |
| 我答了一半想换维度 | 默认是顺序执行 | 说"跳到 D4-A1"，确认问题ID后在 references/framework-full.md 里查 |
| 我想看看之前答了什么 | 没有自动查询 | 说"查看 D1-A1 的回答" |

---

*开始之前，记住一件事：这不是考试，没有"正确答案"。你越坦诚地面对自己不完美的部分，得到的操作手册就越有用。*
